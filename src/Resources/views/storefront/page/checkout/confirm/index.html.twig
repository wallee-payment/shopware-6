{% sw_extends '@Storefront/storefront/page/checkout/confirm/index.html.twig' %}

{% block base_body_script %}
    {{ parent() }}

    {# Provide WhitelabelMachineName integration data if available in page extensions #}
    {% if page.extensions.walleeData %}
        {% set wleData = page.extensions.walleeData %}

        {# Hidden inputs required by the JS components #}
        <input type="hidden" id="cartRecreateUrl" value="{{ wleData.cartRecreateUrl }}" />
        <input type="hidden" id="checkoutUrl" value="{{ wleData.checkoutUrl }}" />

        {# Load WhitelabelMachineName SDK scripts #}
        {% if wleData.deviceJavascriptUrl %}
            <script src="{{ wleData.deviceJavascriptUrl }}" async="async"></script>
        {% endif %}
        {% if wleData.javascriptUrl %}
            <script src="{{ wleData.javascriptUrl }}"></script>
        {% endif %}

        {# We don't need app.js if we use the modern plugin, but we might keep it for now if other pages depend on it.
           However, for the confirm page, the modern plugin registered in main.js will take over
           whenever it finds [data-wallee-checkout-plugin]. #}
    {% endif %}
{% endblock %}
